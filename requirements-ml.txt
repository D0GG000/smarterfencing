# ML stack notes (create conda env, then pip/mim — mirrors Dockerfile.base)
#
#   conda create -y -n mmpose-env python=3.8
#   conda activate mmpose-env
#   conda install -y pytorch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 pytorch-cuda=11.8 -c pytorch -c nvidia
#   pip install -U pip setuptools wheel
#   pip install numpy==1.24.3 opencv-python-headless pandas scipy shapely pycocotools timm easydict tensorboardX
#   pip install -U openmim
#   mim install mmengine
#   mim install "mmcv==2.1.0"
#   mim install mmdet mmpretrain mmpose
#   pip install -r requirements-web.txt
#
# Then:
#   ./start_local_webapp.sh
#   # or: .\start_local_webapp.ps1
numpy==1.24.3
opencv-python-headless
pandas
scipy
shapely
pycocotools
timm
easydict
tensorboardX
openmim
